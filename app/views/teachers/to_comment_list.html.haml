%h1 To comment forms
%table
  %thead
    %tr
      %th Name
      %th Education
      %th Comments

      
  %tbody
    - @list.each do |form|
      %tr
        %td= form.name
        %td= form.education
        %td= link_to 'Add Comments', teachers_comments_path(:id => @user.id, :form_id => form.id)
